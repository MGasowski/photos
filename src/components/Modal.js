const Modal = (props) => {
  return (
    <div class=" overflow-y-auto overflow-x-hidden fixed right-0 left-0 top-4 z-50 justify-center items-center h-modal md:h-full md:inset-0 bg-cyan-300">
      sss
    </div>
  );
};

export default Modal;
